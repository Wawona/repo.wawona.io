---
description: repo.wawona.io dual channel /wasm (Mode A) vs /jailbreak (Mode B); firewall
alwaysApply: true
---

# This repo: `repo.wawona.io`

One host, **two channels**. Mixing indexes or auto-discovery across channels is
a compliance bug.

```text
/search/                 → humans: choose Mode A wasm or Mode B debs. Never a mixed list.
/search/?channel=wasm    → humans: App Store / Play wasm catalog
/search/?channel=deb     → humans: jailbreak Sileo debs
/wasm/v1/                → Mode A: WASI packages for Wawona Runtime (store-safe CDN data)
/                        → Sileo APT (Packages + debs). Do not move for the catalog.
/jailbreak/              → human landing onto the deb catalog. Not a second APT tree.
```

`/wasm/`, `/deb/`, and `/jailbreak/` HTML redirect to `/search/?channel=…`. Never
redirect `/wasm/v1/` or `/Packages`. The website may link both catalogs. It must
not show them as one results list. Store `wpm` must never see debs.

## Hard firewall

| Consumer | `/wasm/` | `/jailbreak/` |
|---|---|---|
| App Store / Play / `wpm` in store builds | Yes | **Never** |
| Sileo / jailbroken devices | Optional | Yes |
| Website Mode B pages | Optional | Yes |

## Also publish (Mode B)

- Auto-package **Wawona iOS Mode B IPA** for Sileo (distinct from App Store IPA).
- Desktop / LockScreen / Swinging Bridge Mode B **tweaks** as `.deb`, not as
  Wasm packages.

## Never

- One index listing both `.wasm` and `.deb`
- Teach store `wpm` to probe `/jailbreak/`
- Claim Wawona Runtime needs Mode B to install packages (Runtime is Mode A only)

See `wawona-product-map` and `Wawona/docs/wasm-package-manager.md`.
